/// @description randomize sprite
image_index = irandom_range(0, 9);

for (var i = 0; i < irandom_range(0,1); i++) {
	image_xscale = -1;
}
xScale = image_xscale;

//event_inherited();
//polygon =polygon_from_instance(id);
// and set parent to obj_light_renderer